const SubheadingN = ({ title, styles }) => {
    return (
        <h3 className={`  text-grey-900 text-lg   font-semibold  ${styles}`}>
            {title}
        </h3>
    )
}
export default SubheadingN;